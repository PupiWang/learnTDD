plugins {
    id 'java'
}

sourceCompatibility = 1.5
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {

    compile (
    )

    testCompile (
            ['junit:junit:4.11'],
            ['org.mockito:mockito-all:1.9.5'],
    )

}